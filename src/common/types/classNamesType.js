//
// Declare support of the `classnames` lib, for example when passing a `className`
// prop in a component that can be more than a simple string.
//
export type classNamesType = string | {[key: string]: any} | Array<string>

export default classNamesType
